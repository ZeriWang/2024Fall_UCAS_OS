# 作业11

## 姓名：王泽黎 &nbsp; 学号：2022K8009929011

### 11.1

（1）由于RAID-5可以并行读取数据，所以读取一个条带的时间基本等于读取单个块的时间。则读取一个条带的时间为：

$$总时间 = 寻道时间 + 旋转延迟 + 传输时间 = 4ms + 4.17ms + 0.0195ms ≈ 8.19ms$$

（2）

- 同一条带情况
  - 需要4次磁盘操作，则总时间为：$4 \times 8.19ms = 32.76ms$
- 不同条带情况
  - 需要8次磁盘操作，则总时间为：$8 \times 8.19ms = 65.52ms$

### 11.2

假设逻辑块与物理页都为 4 KB，则有：

- $总可写入量 = 320GB * 200000 = 64000000GB$
- $每秒写入量 = 300,000 * 4KB = 1.14GB/s$
- $寿命 = \frac{64000000GB}{1.14GB/s} = 56140350s ≈ 651.2天$

### 11.3

（1）Switch merge: 2000 ms（擦除块操作）
（2）Partial merge (40%有效页): $52 * 25 ms（读取有效页） + 52 * 80 ms（擦除块操作） + 2000 ms（擦除块操作） = 7460 ms$
（3）Full merge: $128 * 25 ms（读取 128 页） + 128 * 80（写入 128 页） + 2 * 2000 ms（两次擦除块操作） = 17440 ms$